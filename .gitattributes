# Normalize line endings to LF in the repo. Prevents CRLF churn on Windows and
# keeps CI (Linux) diffs clean. Checkout can still be CRLF locally.
* text=auto eol=lf

# Binary / generated artefacts — never normalize.
*.png binary
*.parquet binary
uv.lock -text
