"""The semantic layer: metrics defined in YAML, compiled to safe parameterised
SQL. This is what lets the LLM ask for a *metric* instead of inventing SQL, so it
cannot fabricate a wrong revenue definition."""
