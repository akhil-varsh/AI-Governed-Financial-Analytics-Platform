{
  "//": "Copy the 'cfo-mcp' block into your Claude Desktop config and adjust the path.",
  "//_config_location_macos": "~/Library/Application Support/Claude/claude_desktop_config.json",
  "//_config_location_windows": "%APPDATA%\\Claude\\claude_desktop_config.json",
  "//_note": "Run `make export` once first so data/warehouse.duckdb exists. Restart Claude Desktop after editing.",
  "mcpServers": {
    "cfo-mcp": {
      "command": "uv",
      "args": [
        "--directory",
        "C:/Users/Akhil/Python_Projects/Data Engineering/northwind-lakehouse/mcp-server",
        "run",
        "cfo-mcp"
      ],
      "env": {}
    }
  }
}
